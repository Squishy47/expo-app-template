meta {
  name: create user
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/sign-up/email
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Local Test User",
    "email": "test@example.com",
    "password": "password1234"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
